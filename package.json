{
  "name": "icaif-2026-website",
  "private": true,
  "scripts": {
    "build": "npm run build:css && npm run check",
    "build:css": "tailwindcss -i ./css/tailwind.input.css -o ./css/tailwind.min.css --minify",
    "check:html": "html-validate '*.html'",
    "check:js": "node --check js/heroicons.js && node --check js/home-stats.js && node --check js/important-dates.js && node --check js/site-layout.js && node --check scripts/check-site.js",
    "check:site": "node scripts/check-site.js",
    "check": "npm run check:js && npm run check:html && npm run check:site"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "daisyui": "^5.7.22",
    "html-validate": "10.0.0",
    "tailwindcss": "^4.3.3"
  }
}
